apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: llm-telemetry-gateway-policy
  namespace: gateway
spec:
  podSelector:
    matchLabels:
      app: llm-telemetry-gateway
  policyTypes:
  - Ingress
  ingress:
  - ports:
    - protocol: TCP
      port: 8080
